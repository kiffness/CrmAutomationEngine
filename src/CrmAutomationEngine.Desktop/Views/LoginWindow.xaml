<Window x:Class="CrmAutomationEngine.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CRM Automation Engine — Login"
        Height="220" Width="360"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>

    <StackPanel Margin="24" VerticalAlignment="Center">

        <TextBlock Text="Email" Margin="0,0,0,4"/>
        <TextBox x:Name="EmailBox"
                 Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,12"/>

        <TextBlock Text="Password" Margin="0,0,0,4"/>
        <PasswordBox x:Name="PasswordBox" Margin="0,0,0,12"/>

        <TextBlock Text="{Binding Error}" Foreground="Red"
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}"
                   Margin="0,0,0,8" TextWrapping="Wrap"/>

        <Button Content="Log In"
                Command="{Binding LoginCommand}"
                CommandParameter="{Binding ElementName=PasswordBox, Path=Password}"
                IsDefault="True"
                Padding="8,4"/>

    </StackPanel>
</Window>
