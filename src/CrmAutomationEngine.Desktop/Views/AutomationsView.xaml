<UserControl x:Class="CrmAutomationEngine.Desktop.Views.AutomationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- List -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
                <Button Content="Load" Command="{Binding LoadCommand}" Margin="0,0,5,0"/>
                <Button Content="Add"  Command="{Binding AddCommand}"/>
            </StackPanel>

            <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name"     Binding="{Binding Name}"         Width="*"/>
                    <DataGridTextColumn Header="Trigger"  Binding="{Binding Trigger}"       Width="80"/>
                    <DataGridTextColumn Header="Template" Binding="{Binding EmailTemplate.Name}" Width="160"/>
                    <DataGridTextColumn Header="Delay (min)" Binding="{Binding DelayMinutes}" Width="90"/>
                    <DataGridTemplateColumn Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit"
                                            Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Margin="0,0,4,0"/>
                                    <Button Content="Delete"
                                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
                <Button Content="&#8592; Prev" Command="{Binding PrevPageCommand}" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding PageDisplay}" VerticalAlignment="Center"/>
                <Button Content="Next &#8594;" Command="{Binding NextPageCommand}" Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Edit form -->
        <Border Grid.Column="1" Width="320" Padding="12"
                BorderBrush="LightGray" BorderThickness="1,0,0,0"
                Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <TextBlock Text="{Binding FormTitle}" FontWeight="Bold" Margin="0,0,0,10"/>

                <TextBlock Text="Name"/>
                <TextBox Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,8"/>

                <TextBlock Text="Trigger"/>
                <ComboBox ItemsSource="{Binding TriggerOptions}"
                          SelectedValuePath="Value"
                          DisplayMemberPath="Label"
                          SelectedValue="{Binding EditTrigger}"
                          Margin="0,2,0,8"/>

                <TextBlock Text="Email Template"/>
                <ComboBox ItemsSource="{Binding AvailableTemplates}"
                          DisplayMemberPath="Name"
                          SelectedItem="{Binding EditTemplate}"
                          Margin="0,2,0,8"/>

                <TextBlock Text="Delay (minutes)"/>
                <TextBox Text="{Binding EditDelayMinutes, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,8"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red"
                           TextWrapping="Wrap" Margin="0,0,0,8"/>

                <StackPanel Orientation="Horizontal">
                    <Button Content="Save"   Command="{Binding SaveCommand}"   Margin="0,0,5,0"/>
                    <Button Content="Cancel" Command="{Binding CancelCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
