<UserControl x:Class="CrmAutomationEngine.Desktop.Views.TemplatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- List -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding LoadError}" Foreground="White" Background="#CC3300"
                       Padding="8,4" TextWrapping="Wrap"
                       Visibility="{Binding HasLoadError, Converter={StaticResource BoolToVis}}"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
                <Button Content="Load" Command="{Binding LoadCommand}" Margin="0,0,5,0"/>
                <Button Content="Add"  Command="{Binding AddCommand}"/>
            </StackPanel>

            <DataGrid Grid.Row="2" ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name"    Binding="{Binding Name}"    Width="*"/>
                    <DataGridTextColumn Header="Updated" Binding="{Binding UpdatedAt, StringFormat='{}{0:g}'}" Width="140"/>
                    <DataGridTemplateColumn Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit"
                                            Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Margin="0,0,4,0"/>
                                    <Button Content="Delete"
                                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
                <Button Content="&#8592; Prev" Command="{Binding PrevPageCommand}" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding PageDisplay}" VerticalAlignment="Center"/>
                <Button Content="Next &#8594;" Command="{Binding NextPageCommand}" Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Edit form -->
        <Border Grid.Column="1" Width="320" Padding="12"
                BorderBrush="LightGray" BorderThickness="1,0,0,0"
                Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <TextBlock Text="{Binding FormTitle}" FontWeight="Bold" Margin="0,0,0,10"/>

                <TextBlock Text="Name"/>
                <TextBox Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,8"/>

                <TextBlock Text="HTML Body"/>
                <TextBox Text="{Binding EditHtmlBody, UpdateSourceTrigger=PropertyChanged}"
                         Height="220" TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" FontFamily="Consolas"
                         Margin="0,2,0,8"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red"
                           TextWrapping="Wrap" Margin="0,0,0,8"/>

                <StackPanel Orientation="Horizontal">
                    <Button Content="Save"   Command="{Binding SaveCommand}"   Margin="0,0,5,0"/>
                    <Button Content="Cancel" Command="{Binding CancelCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
